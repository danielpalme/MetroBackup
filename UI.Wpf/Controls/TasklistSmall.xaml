<UserControl x:Class="Palmmedia.BackUp.UI.Wpf.Controls.TasklistSmall"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="35" d:DesignWidth="180">

    <Grid Margin="0,5,0,0" >
        <Label Content="{Binding Path=Name, UpdateSourceTrigger=PropertyChanged}" FontSize="20" FontWeight="Light" VerticalAlignment="Top" HorizontalAlignment="Stretch" />
        <Separator Margin="0,35,0,0" Foreground="{StaticResource BorderBrush}" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
